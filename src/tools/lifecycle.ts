// ── Lifecycle Tools ── (Phase 2-3)
// velixar_distill, velixar_batch_store, velixar_batch_search, velixar_consolidate, velixar_retag

import type { Tool } from '@modelcontextprotocol/sdk/types.js';

export const lifecycleTools: Tool[] = [];
// Phase 2: velixar_distill
// Phase 3: batch, consolidate, retag, session
