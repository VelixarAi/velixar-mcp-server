name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: velixarai/velixar-ci-workflows/.github/workflows/node-ci.yml@main
    with:
      node-version: '20'
    secrets: inherit
